import "../styles/SectionTitle.css"

export function SectionTitle() {
    return (
        <div className="section-title">
            <h2>Resume (test deploy on AWS)</h2>
            <p>Check My Resume</p>
        </div>
    )
}